// const move = (row, colomn) => `\x1b[${row};${colomn}H`;
// const encoder = new TextEncoder();

// for (let index = 0; index < 100; index++) {
//   for (let j = 0; j < 190; j++) {
//     Deno.stdout.write(encoder.encode(move(index, j) + "O"));
//   }
// }
